/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "tmptglytfnsbtmqlarrc.supabase.co"
      },
    ],
  },
};

module.exports = nextConfig;
